$(start)
function selectoption(itemId,itemName){
    let selectoption = $("<option>");
    $("select").append(selectoption);
    selectoption.val(itemId);
    selectoption.html(itemName);
    $(".out").append(selectoption);
    return
}
function createTable(){
    let 
}
function start(){
    
}